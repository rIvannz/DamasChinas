<Page x:Class="DamasChinas_Client.UI.Pages.Friends"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{DynamicResource SurfaceBrush}"
      d:DesignHeight="720" d:DesignWidth="1280">

    <Grid>
        <!-- Fondo -->
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/DamasChinas_Client.UI;component/Assets/Backgrounds/board-background.png"
                        Stretch="UniformToFill"/>
        </Grid.Background>

        <Rectangle Fill="#66000000"/>

        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Encabezado -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Width="60" Height="60"
                        Background="Transparent"
                        BorderThickness="0"
                        ToolTip="{DynamicResource back}"
                        Cursor="Hand"
                        Click="OnBackClick">
                    <Image Source="pack://application:,,,/DamasChinas_Client.UI;component/Assets/Icons/backIcon.png"
                           Width="40" Height="40"
                           Stretch="Uniform"/>
                </Button>

                <TextBlock Grid.Column="1"
                           Text="{DynamicResource friendsList}"
                           Foreground="White"
                           FontSize="58"
                           FontFamily="pack://application:,,,/DamasChinas_Client.UI;component/Assets/Fonts/#Oups"
                           FontWeight="Bold"
                           TextAlignment="Center"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">

                    <Button Width="150" Height="40"
                            Margin="0,0,10,0"
                            Style="{DynamicResource BaseGameButton}"
                            Content="{DynamicResource addFriend}"
                            Click="OnAddFriendClick"/>

                    <Button Width="190" Height="40"
                            Style="{DynamicResource BaseGameButton}"
                            Content="{DynamicResource pendingFriendRequests}"
                            Click="OnViewPendingRequestsClick"/>

                </StackPanel>
            </Grid>

            <!-- Lista -->
            <Border Grid.Row="1"
                    Background="#80222222"
                    BorderBrush="#444"
                    BorderThickness="1"
                    CornerRadius="10"
                    Padding="20">

                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">

                    <ItemsControl ItemsSource="{Binding FriendsList}" Margin="10">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>

                                <Border Background="#44222222"
                                        BorderBrush="#666"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="10"
                                        Margin="0,0,0,10">

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Estado -->
                                        <Ellipse Width="16" Height="16"
                                                 VerticalAlignment="Center"
                                                 Margin="5,0,10,0"
                                                 Fill="{Binding Status, Converter={StaticResource StatusToColor}}"
                                                 Stroke="Black"
                                                 StrokeThickness="1"/>

                                        <!-- Avatar -->
                                        <Image Grid.Column="1"
                                               Source="{Binding Avatar}"
                                               Width="45" Height="45"
                                               Stretch="UniformToFill"
                                               Margin="0,0,15,0"
                                               ClipToBounds="True"/>

                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding Username}"
                                                   Foreground="White"
                                                   FontSize="18"
                                                   VerticalAlignment="Center"/>

                                        <Button Grid.Column="3"
                                                Width="130"
                                                Height="40"
                                                Style="{DynamicResource BaseGameButton}"
                                                Content="{DynamicResource profile}"
                                                Margin="10,0,10,0"
                                                Click="OnViewProfileClick"/>

                                        <Button Grid.Column="4"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                ToolTip="{DynamicResource chat}"
                                                Click="OnChatClick">

                                            <Image Source="pack://application:,,,/DamasChinas_Client.UI;component/Assets/Icons/chatIcon.png"
                                                   Width="30"
                                                   Height="30"/>
                                        </Button>

                                    </Grid>

                                </Border>

                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </ScrollViewer>
            </Border>

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom">
                <Button Width="44" Height="44"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="OnSoundClick">
                    <Image Source="pack://application:,,,/DamasChinas_Client.UI;component/Assets/Icons/soundIcon.png"
                           Width="26" Height="26"/>
                </Button>

                <Button Width="44" Height="44"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="OnLanguageClick">
                    <Image Source="pack://application:,,,/DamasChinas_Client.UI;component/Assets/Icons/languageIcon.png"
                           Width="26" Height="26"/>
                </Button>
            </StackPanel>

        </Grid>
    </Grid>
</Page>
